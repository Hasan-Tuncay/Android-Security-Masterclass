package com.hasantuncay.mobsec.common.models

/**
 * Marker interface for secure mitigation strategies.
 * Used by BaseSecureScreen / BaseMasweScreen(mode = SECURE).
 */
interface MasweMitigation : MasweItem
